// Diferença entre var e let

// var nome = 'Lucas';
// var nome = 'Otávio'; // Com var isso não gera erro

// nome = 'Luiz'; // NÃO FAÇA ISSO

// console.log(nome);

// TIPOS DE DADOS
// String, number, undefined, boolean, null, symbol
const nome = 'Lucas'; // string
const num1 = 1; // number
const num2 = 1.111; // number
let nomeAluno; // undefined = não aponta para local nenhum na memória
const sobrenomeAluno = null; // Nulo -> não aponta para local nenhum na memória
const boolean = true; // Boolean = true, false (lógico)